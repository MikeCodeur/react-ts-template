import {SimpleGrid as ChakraSimpleGrid, SimpleGridProps} from '@chakra-ui/react'

export const SimpleGrid = ({...rest}: SimpleGridProps) => {
  return <ChakraSimpleGrid {...rest}></ChakraSimpleGrid>
}
export default SimpleGrid

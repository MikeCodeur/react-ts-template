import { GridItem as ChakraGridItem } from "@chakra-ui/react"

export const GridItem = ({
  ...rest
}) => {
  
  return(
    <ChakraGridItem {...rest}>

    </ChakraGridItem>
  )

}
export default GridItem;